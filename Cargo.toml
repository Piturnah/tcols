[package]
name = "tcols"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
crossterm = { git = "http://github.com/Piturnah/crossterm", branch = "strum", features = ["strum"] } 
strum = "0.24"
